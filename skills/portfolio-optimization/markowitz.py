# Markowitz Portfolio mean-variance weights optimizer
def optimize_portfolio_weights(returns, covariance_matrix):
    # Simulated weights optimization
    num_assets = len(returns)
    equal_weights = [1.0 / num_assets] * num_assets
    return equal_weights
